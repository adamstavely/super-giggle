apiVersion: v1
kind: Service
metadata:
  name: {{ include "intranet-search.fullname" . }}
  labels:
    {{- include "intranet-search.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "intranet-search.selectorLabels" . | nindent 4 }}
